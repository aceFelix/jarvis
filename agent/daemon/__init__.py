"""贾维斯常驻模式（Daemon Mode）。

让贾维斯后台常驻，通过全局热键或系统托盘随时唤起，"退下"后回到后台待命。
这是阶段五"贾维斯形态"的第一刀——让贾维斯"一直在"。

两种唤起方式（可同时启用）:
1. **全局热键**: 默认 Ctrl+Shift+J，随时唤起语音对话
2. **系统托盘**: 右键菜单选择"对话/语音/退出"

依赖（均优雅降级，缺一个不影响另一个）:
- `keyboard` 库: 全局热键监听。Windows 上需要管理员权限注册部分热键。
- `pystray` + `PIL`: 系统托盘图标。无则跳过托盘。

生命周期:
    启动 daemon → 后台待命（托盘图标 + 热键监听）
      ├─ 热键按下 / 托盘"语音" → 唤起语音对话循环（voice_loop）
      ├─ 托盘"对话" → 唤起文本 REPL（一次性，exit 回后台）
      └─ 托盘"退出" / Ctrl+C → 彻底退出
"""

from agent.daemon.daemon import JarvisDaemon

__all__ = ["JarvisDaemon"]
